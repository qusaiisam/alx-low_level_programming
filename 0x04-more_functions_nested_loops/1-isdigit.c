#include "main.h"
/**
* _isdigit - cheks if digit
* @c: character to check if digit
* Description: this function prints '_putchar'
* Return: always return 0
**/
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)

		return (1);
	else
		return (0);
}
